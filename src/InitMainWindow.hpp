// #include <QMainWindow>
// #include <QLineEdit> // Für die Eingabe

// class MyWindow : public QMainWindow {
//     private:
//     Q_OBJECT // Wichtig! Ermöglicht Signals & Slots
// public:
//     virtual explicit MyWindow(QWidget *parent = nullptr) = 0;
//     virtual ~MyWindow() = 0;
// }